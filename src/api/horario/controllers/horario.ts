import { factories } from '@strapi/strapi';

/**
 * horario controller
 *
 * Keeping the default core controller so we can extend it once the data
 * structure is defined.
 */
export default factories.createCoreController('api::horario.horario');
